{
  "name": "generating-og-image",
  "version": "1.0.0",
  "description": "Generate an OG Image",
  "main": "dist/card.js",
  "scripts": {
    "build": "AWS_LAMBDA_FUNCTION_NAME=true tsc",
    "now-build": "AWS_LAMBDA_FUNCTION_NAME=true tsc",
    "watch": "AWS_LAMBDA_FUNCTION_NAME=true tsc --watch"
  },
  "license": "MIT",
  "dependencies": {
    "chrome-aws-lambda": "8.0.2",
    "puppeteer-core": "8.0.0"
  },
  "devDependencies": {
    "@types/node": "^13.5.1",
    "typescript": "3.7.5"
  }
}
